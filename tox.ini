# content of: tox.ini , put in same dir as setup.py
[tox]
envlist=flake8,py27,py26

[testenv]
deps =
    -rdev-requirements.txt
commands=sudo nosetests --with-cov --cov-report term-missing --cov serv serv/tests -v

[testenv:flake8]
deps =
    flake8
    -rdev-requirements.txt
commands=flake8 serv
